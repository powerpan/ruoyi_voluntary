# P5-H 阶段验收记录

## 1. 验收范围

本次验收范围是 P5-H：对 P5 统计、通知与导出阶段进行收口验证。

覆盖内容：

1. 管理端统计总览、活动统计、志愿者排行、组织统计和服务趋势。
2. 管理端通知记录查询。
3. 用户端我的通知、未读数、已读状态和通知跳转。
4. 志愿者档案、报名名单、签到签退记录、服务记录四类导出。
5. P5 临时数据整理和下一阶段边界确认。

不在本次范围：

1. P6 最终部署说明、截图清单和论文支撑材料。
2. 外部短信、邮件、微信、App 推送。
3. 组织管理员数据权限隔离。
4. 异步导出任务、复杂 BI 大屏和服务证明 PDF。

## 2. 当前运行环境

本次验收基于当前本地运行态：

| 服务 | 地址 | 状态 |
| --- | --- | --- |
| 后端 | `http://localhost:8080` | 已运行 |
| 管理端 | `http://127.0.0.1:8081` | 已运行 |
| 用户端 | `http://localhost:8088` | 已运行 |

验证账号：

| 类型 | 账号 | 用途 |
| --- | --- | --- |
| 管理员 | `admin / admin123` | 管理端统计、通知、导出入口验证 |
| 志愿者 | `p3g_demo_volunteer / 123456` | 用户端通知中心验证 |

## 3. 统计验收

### 3.1 管理端统计页

页面地址：`/voluntary/statistics`

浏览器验证结果：

| 区域 | 结果 |
| --- | --- |
| 统计总览 | 页面可打开，展示志愿者、活动、报名、签到签退、有效服务记录和异常记录 |
| 活动统计 | 展示 `P3-H闭环验收活动` 和 `P3-G用户端演示活动` 两条活动统计 |
| 志愿者排行 | 页签可切换，展示志愿者维度服务排行 |
| 组织统计 | 页签可切换，展示组织维度服务统计 |
| 服务趋势 | 页签可切换，趋势图 canvas 可渲染 |

当前库页面统计快照：

| 指标 | 页面值 |
| --- | --- |
| 志愿者总数 | 2 |
| 已审核志愿者 | 2 |
| 活动总数 | 2 |
| 已发布活动 | 2 |
| 报名总数 | 3 |
| 报名通过率 | 66.7% |
| 签到 / 签退 | 1 / 1 |
| 有效服务记录 | 1 |
| 累计服务时长 | 1 分钟 |
| 异常记录 | 0 |

控制台结果：无新增错误；仅保留参考工程已有的 `vue-router` 非嵌套路由 warning。

### 3.2 统计口径判断

当前统计数据与 P3/P4 保留的演示数据一致：

1. `P3-H闭环验收活动` 有报名、签到、签退和 1 条有效服务记录。
2. `P3-G用户端演示活动` 有报名记录，但未产生签到签退和服务记录。
3. `p3g_demo_volunteer` 的自动验收服务记录已清理，服务时长仍为 `0 / 0`。
4. `powerpan / eric` 的手工验证数据保留，服务时长为 `1 / 1`。

## 4. 通知验收

### 4.1 管理端通知记录

页面地址：`/voluntary/notification`

浏览器验证结果：

| 场景 | 结果 |
| --- | --- |
| 页面打开 | 成功 |
| 筛选区 | 接收人、标题、类型、状态、业务对象、创建时间筛选项可见 |
| 通知列表 | 可看到 `p3g_demo_volunteer` 的 2 条志愿者审核通知 |
| 阅读状态 | 两条通知均为已读 |
| 详情入口 | 每条通知均展示详情按钮 |

本次保留的通知数据：

| 接收人 | 通知 | 状态 | 用途 |
| --- | --- | --- | --- |
| `p3g_demo_volunteer` | 志愿者档案审核驳回 | 已读 | 用户端通知演示 |
| `p3g_demo_volunteer` | 志愿者档案审核通过 | 已读 | 用户端通知演示 |

### 4.2 用户端通知中心

页面地址：`/notifications`

浏览器验证结果：

| 场景 | 结果 |
| --- | --- |
| 页面打开 | 成功 |
| 当前用户 | `p3g_demo_volunteer` |
| 通知列表 | 显示 2 条自己的志愿者审核通知 |
| 未读数 | 0 |
| 已读状态 | 两条通知均展示已读和阅读时间 |
| 通知跳转 | “查看”可跳转到 `/me` |

说明：用户端接口的“只能查看和标记当前登录用户自己的通知”边界已在 P5-C 接口阶段验证。本次 P5-H 运行态复核确认用户端只展示当前账号 `p3g_demo_volunteer` 的通知记录。

## 5. 导出验收

### 5.1 页面入口

四个管理端页面均已确认导出按钮可见，且页面列表或空状态正常渲染。

| 页面 | 地址 | 导出入口 |
| --- | --- | --- |
| 志愿者审核 | `/voluntary/volunteer` | 可见 |
| 报名管理 | `/voluntary/signup` | 可见 |
| 签到管理 | `/voluntary/checkin` | 可见 |
| 服务记录 | `/voluntary/serviceRecord` | 可见 |

### 5.2 后端导出能力

本次新增并执行导出响应测试，覆盖四类导出对象：

| 导出对象 | 验证内容 |
| --- | --- |
| `VolVolunteerProfileExport` | 能生成 `.xlsx` 响应，包含正确 Content-Type 和 ZIP 文件头 |
| `VolActivitySignupExport` | 能生成 `.xlsx` 响应，包含正确 Content-Type 和 ZIP 文件头 |
| `VolCheckinRecordExport` | 能生成 `.xlsx` 响应，包含正确 Content-Type 和 ZIP 文件头 |
| `VolServiceRecordExport` | 能生成 `.xlsx` 响应，包含正确 Content-Type 和 ZIP 文件头 |

测试命令：

```bash
cd /Users/ericpan/game_project/ruoyi_voluntary
mvn -pl voluntary-system -am test
```

结果：

```text
VolBusinessNotificationServiceTest
Tests run: 3, Failures: 0, Errors: 0, Skipped: 0

VolExportDefinitionTest
Tests run: 2, Failures: 0, Errors: 0, Skipped: 0

Reactor Summary for ruoyi-voluntary 3.8.7:
ruoyi-common ........................... SUCCESS
ruoyi-system ........................... SUCCESS
ruoyi-framework ........................ SUCCESS
voluntary-system ....................... SUCCESS
BUILD SUCCESS
```

说明：Codex 内置浏览器不支持下载事件，当前命令行沙箱也禁止直接新建到本地后端端口的 HTTP 连接，因此本次用“页面导出入口 + 后端 `.xlsx` 响应测试”作为自动化验收依据。普通浏览器手工点击下载可作为后续演示前的人工复核项。

## 6. 数据整理

本次没有新增临时活动、报名、签到或服务记录。

保留数据：

1. `P3-H闭环验收活动` 相关演示数据。
2. `P3-G用户端演示活动` 相关演示数据。
3. `powerpan / eric` 的 1 分钟手工服务记录。
4. `p3g_demo_volunteer` 的 2 条已读通知，作为通知中心演示数据。

已确认：

1. `p3g_demo_volunteer` 最终审核状态为通过。
2. `p3g_demo_volunteer` 未读通知数为 0。
3. P5-H 未产生需要额外清理的临时业务数据。

## 7. P5 结论

P5 统计、通知与导出阶段验收通过。

已完成：

1. 统计总览和多维统计页面。
2. 业务通知表、通知后端接口、业务事件通知写入。
3. 用户端通知中心和顶部未读数。
4. 管理端通知记录查询。
5. 四类业务数据导出接口和页面入口。
6. 导出字段和 `.xlsx` 响应自动化测试。

下一步进入 P6：

1. 全量后端编译和前端构建。
2. 数据库初始化脚本最终验证。
3. 本地部署说明和启动说明整理。
4. 核心流程截图清单。
5. 论文支撑材料、测试用例和最终 README 收口。
